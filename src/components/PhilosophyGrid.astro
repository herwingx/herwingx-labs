---
import LabIcon from "./LabIcon.astro";

const principles = [
  {
    title: "Aprendo publicando",
    description:
      "Escribir es la mejor forma de entender lo que sabes. Publicar me obliga a ser preciso, no solo a creer que entiendo algo.",
    icon: "productivity",
    featured: false,
  },
  {
    title: "Opino con criterio",
    description:
      "No comparto listas neutras de opciones. Cada artículo tiene un punto de vista basado en experiencia real y contexto concreto.",
    icon: "toolset",
    featured: false,
  },
  {
    title: "Contexto antes que código",
    description:
      "El código sin contexto es ruido. Me interesa el por qué detrás de cada decisión técnica, no solo el cómo implementarla.",
    icon: "mastery",
    featured: false,
  },
  {
    title: "Solo lo que funciona",
    description:
      "Lo que publico ha pasado por uso real. Sin hype, sin teoría vacía — solo cosas que probé y que volvería a usar.",
    icon: "consistency",
    featured: true,
  },
];
---

<div class="philosophy-grid">
  {
    principles.map((p, i) => (
      <div class={`philosophy-card philosophy-card--${i}${p.featured ? " philosophy-card--featured" : ""}`}>
        <div class="card-header">
          <div class="icon-container">
            <LabIcon name={p.icon as any} size="28px" />
          </div>
          <h3>{p.title}</h3>
        </div>
        <p>{p.description}</p>
      </div>
    ))
  }
</div>

<style>
  /*
    Bento en espejo vs MasteryGrid:
    [ Productivity (1col) ] [  Elite Toolset (2cols)  ]
    [  Technical Mastery (2cols)  ] [ Consistencia (1col) ★ ]
  */
  .philosophy-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: auto auto;
    gap: 1.25rem;
    margin: 2rem 0;
  }

  /* Productivity: col 1, fila 1 */
  .philosophy-card--0 { grid-column: 1 / 2; }

  /* Elite Toolset: cols 2-3, fila 1 */
  .philosophy-card--1 { grid-column: 2 / 4; }

  /* Technical Mastery: cols 1-2, fila 2 */
  .philosophy-card--2 { grid-column: 1 / 3; }

  /* Consistencia Real: col 3, fila 2 */
  .philosophy-card--3 { grid-column: 3 / 4; }

  .philosophy-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 2rem;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    min-height: 160px;
  }

  .philosophy-card:hover {
    border-color: rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.04);
    transform: scale(1.02);
  }

  /* Card destacado (Consistencia Real) */
  .philosophy-card--featured {
    background: linear-gradient(
      135deg,
      rgba(124, 58, 237, 0.15) 0%,
      rgba(59, 130, 246, 0.08) 100%
    );
    border-color: rgba(124, 58, 237, 0.4);
  }

  .philosophy-card--featured:hover {
    border-color: rgba(124, 58, 237, 0.7);
    background: linear-gradient(
      135deg,
      rgba(124, 58, 237, 0.22) 0%,
      rgba(59, 130, 246, 0.12) 100%
    );
  }

  /* Cards grandes */
  .philosophy-card--1 h3,
  .philosophy-card--2 h3 {
    font-size: 1.3rem !important;
  }

  .card-header {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .icon-container {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  h3 {
    margin: 0 !important;
    font-size: 1.1rem !important;
    font-weight: 700 !important;
    color: var(--color-text) !important;
    font-family: var(--sl-font-mono) !important;
  }

  p {
    margin: 0 !important;
    font-size: 0.95rem !important;
    color: var(--color-text-dim) !important;
    line-height: 1.6 !important;
  }

  /* Responsive: 2 columnas en tablet */
  @media (max-width: 900px) {
    .philosophy-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .philosophy-card--0 { grid-column: 1 / 2; }
    .philosophy-card--1 { grid-column: 2 / 3; }
    .philosophy-card--2 { grid-column: 1 / 3; }
    .philosophy-card--3 { grid-column: 1 / 3; }
  }

  /* Responsive: 1 columna en móvil */
  @media (max-width: 600px) {
    .philosophy-grid {
      grid-template-columns: 1fr;
    }
    .philosophy-card--0,
    .philosophy-card--1,
    .philosophy-card--2,
    .philosophy-card--3 {
      grid-column: 1 / 2;
    }
  }
</style>
